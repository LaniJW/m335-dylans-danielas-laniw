<?xml version="1.0" encoding="utf-8"?>
<android.support.constraint.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="match_parent">

    <android.support.v7.widget.Toolbar
        android:id="@+id/search_bar"
        android:layout_width="match_parent"
        android:layout_height="?attr/actionBarSize"
        android:background="?attr/colorPrimary"
        android:elevation="4dp"
        android:theme="@style/ThemeOverlay.AppCompat.ActionBar"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toTopOf="parent"
        app:popupTheme="@style/ThemeOverlay.AppCompat.Light">


        <LinearLayout
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            android:orientation="horizontal">

            <Button
                android:id="@+id/search_bar_dropdown_button"
                android:layout_width="@dimen/searchBar_button_width"
                android:layout_height="@dimen/searchBar_button_height"
                android:layout_weight="1"
                android:background="@drawable/ic_chevron_right_black_24dp"/>

            <EditText
                android:id="@+id/search_bar_text_field"
                android:layout_width="@dimen/searchBar_textField_width"
                android:layout_height="match_parent"
                android:layout_weight="1"
                android:ems="10"
                android:inputType="textPersonName"
                android:hint="@string/searchBar_hint_text"/>

            <Button
                android:id="@+id/search_bar_search_button"
                android:layout_width="@dimen/searchBar_button_width"
                android:layout_height="@dimen/searchBar_button_height"
                android:layout_marginEnd="@dimen/searchBar_button_marginEnd"
                android:layout_weight="1"
                android:background="@drawable/ic_search_black_24dp" />
        </LinearLayout>

    </android.support.v7.widget.Toolbar>

</android.support.constraint.ConstraintLayout>